BASE_URL=https://automationexercise.com/

# Browser Profile Mode
# Set to 'true' for persistent Chrome profile (saves cookies, extensions, etc.)
# Set to 'false' for incognito mode (clean state, better for parallel execution)
USE_PERSISTENT_PROFILE=false
